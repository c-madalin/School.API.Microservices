# School Web API


A full-stack Student Management system built with **Visual Studio 2026**. This project demonstrates a clean **3-Tier Architecture** using **.NET Web API (C#)** for the backend and **Angular** for the frontend, persisting data in **PostgreSQL**.

### Key Architecture
* **Repository Layer:** Handles pure CRUD operations.
* **Service Layer:** Manages business logic and data validation.
* **Frontend:** Angular SPA utilizing `StudentService` for API communication.

###  Quick Start
**Important:** Always start the **Backend API** first, then launch the **Angular Client**.
Start the App from the ide then go in the cmd adn run "ng serve"

![App Preview](https://github.com/user-attachments/assets/f3c69296-75a3-4508-9c47-400c73ade065)
